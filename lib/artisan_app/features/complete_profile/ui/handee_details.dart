import 'package:flutter/material.dart';

class HandeeDetailsScreen extends StatelessWidget {
  const HandeeDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Handee Details Screen"),
      ),
    );
  }
}
