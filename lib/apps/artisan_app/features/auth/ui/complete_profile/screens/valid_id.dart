import 'package:flutter/material.dart';

class ValidIDScreen extends StatelessWidget {
  const ValidIDScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Valid ID Screen"),
      ),
    );
  }
}
